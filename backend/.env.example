# Copy this to .env and fill in your values
# cPanel sets PORT automatically — do not override it there

# Optional: restrict which origins can call the API
# Leave empty to allow all (fine for a private app)
ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# Only set PORT when running locally outside of cPanel
PORT=3001
